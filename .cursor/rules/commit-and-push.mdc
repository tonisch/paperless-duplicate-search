---
description: Nach Code-Änderungen commit + push; nach Push Server per SSH aktualisieren
alwaysApply: true
---

# Auto-Commit und Server-Update

- **Nach Code-Änderungen:** Alle geänderten Dateien stagen, mit aussagekräftiger Message committen (z. B. auf Deutsch) und zu `origin` pushen.
- **Nach einem Push:** Server aktualisieren mit:
  ```bash
  ssh nancy "cd ~/paperless-duplicate-search && git pull && docker-compose down && docker-compose up -d --build"
  ```
  App-URL: http://10.23.42.21:8023
